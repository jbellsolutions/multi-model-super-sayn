# /health — Repo Health Check

Run a quick health check on the multi-model orchestration setup.

```bash
bash scripts/smoke-test.sh
```

Reports:
- CLI installation status
- Authentication status
- Agent file presence
- MCP config validity
- Routing rules in CLAUDE.md
